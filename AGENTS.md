# Definition of done

A task is done only when all of the following conditions are met:

- The task has an end-to-end (E2E) test that covers the implemented behavior.
- All tests pass.
- All E2E tests pass.
- The `verify` check passes.

Do not mark or report a task as done if any of these conditions is unmet.
